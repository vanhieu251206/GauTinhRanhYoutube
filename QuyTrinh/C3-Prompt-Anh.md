# C3 — Viết Prompt Ảnh

> Đầu vào: `scene-list.md` (từ C2, đã chia scene đầy đủ) + `script.md`/`voice-script.md` (C1).
> Đầu ra: `prompt-anh.txt` (1 dòng = 1 prompt hoàn chỉnh) + ảnh trong `Anh Video/` của thư mục tập.
> Ghi nhớ: kênh dùng **ảnh tĩnh, cắt cứng giữa các ảnh** (KHÔNG zoom/pan, không chuyển động bên trong ảnh) — đúng định dạng đã chốt ở `docs/DNA.md` mục 1. Nguồn style/nhân vật gốc: `docs/Visual-Prompts-goc.md` (charStyle, bgStyle, masterPrompt mẫu). Việc chia scene đã làm xong ở `QuyTrinh/C2-Chia-Scene.md` — KHÔNG tự chia lại ở bước này, chỉ dựng prompt chi tiết cho từng scene đã có sẵn trong `scene-list.md`.

## Nguyên tắc cứng (bắt buộc, không đổi giữa các tập)
- Viết prompt bằng **tiếng Anh**.
- **Gấu KHÔNG bắt buộc xuất hiện trong mọi scene (đã đổi từ quy tắc cũ theo phản hồi thật ở Tập 8 — trước đây ép Gấu vào gần như mọi scene khiến ảnh bị lặp/gượng và làm loãng chất lượng bối cảnh)**: Gấu chỉ cần xuất hiện **định kỳ đủ giữ nhận diện thương hiệu** — bắt buộc có mặt ở Hook, Disclaimer, câu nối chuyển cảnh cố định, Kết luận + catchphrase, cộng thêm rải rác ở vài điểm nhấn quan trọng khác (twist, số liệu gây sốc, chuyển ý lớn) mỗi bước trong 6 bước khung — không cần đúng số lượng cố định, tự cân đối theo độ dài/mật độ ý mỗi tập. **Phần lớn scene còn lại (minh hoạ ví dụ, bối cảnh, case study, số liệu, đồ vật...) ưu tiên tập trung dựng bối cảnh/nội dung thật sinh động và chân thực, không gượng ép nhét Gấu vào để "cho có".**
  - **Lỗi thật đã xảy ra (Tập 8)**: nhiều công cụ gen ảnh có tính năng "character reference"/tham chiếu ảnh trước đó để giữ nhất quán nhân vật — khi bật, tool có thể tự kéo Gấu vào cả những ảnh mà prompt hoàn toàn không nhắc tới. Để phòng ngừa: mọi prompt scene KHÔNG có Gấu bắt buộc thêm câu chặn tường minh cuối prompt: `no bear character, no cartoon animal mascot, no anthropomorphic animal character of any kind, absolutely no character named Gau appearing anywhere in this image.` Đồng thời nhắc người dùng kiểm tra tắt tính năng tham chiếu nhân vật/ảnh trước ở tool gen khi gen các ảnh không cần Gấu, hoặc gen tách riêng 2 đợt (có Gấu / không có Gấu).
  - **Lỗi thật tinh vi hơn đã xảy ra ngay sau đó (Tập 8)**: tool gen ảnh của kênh có tính năng tự khớp ảnh tham chiếu theo TỪ KHOÁ TRÙNG TÊN TỆP xuất hiện trong câu prompt (VD thư mục tham chiếu có file `gau.jpg`, tool quét thấy chữ "Gau" ở bất kỳ đâu trong prompt — kể cả trong câu CHẶN "no character named Gau" — là tự động gán ảnh `gau.jpg` vào, phản tác dụng hoàn toàn). **Do đó, TUYỆT ĐỐI KHÔNG được viết chữ "Gau" (hay bất kỳ từ khoá trùng tên file tham chiếu nào khác) trong câu chặn/phủ định của prompt** — muốn loại trừ Gấu khỏi 1 scene thì diễn đạt hoàn toàn không dùng tên riêng, VD `no bear character, no cartoon animal mascot, no anthropomorphic animal character of any kind appearing anywhere in this image` (không có chữ "Gau"). Áp dụng nguyên tắc này cho MỌI tên riêng nhân vật phụ có ảnh tham chiếu ở `Anh Tham Chieu Nhan Vat/` — không viết tên riêng đó trong câu phủ định của prompt scene không cần nhân vật đó.
- **Bạn (người viết prompt) là đạo diễn, AI chỉ là công cụ vẽ theo đúng chỉ dẫn — không để AI tự quyết bất cứ điều gì đáng lẽ phải chốt sẵn.** Cụ thể: mọi scene có Gấu xuất hiện phải viết rõ TRONG PHẦN MÔ TẢ CHÍNH (không dựa vào câu vị trí khung hình chung chung ở cuối prompt) — (1) Gấu đang làm hành động gì, (2) biểu cảm khuôn mặt cụ thể ra sao (mắt/mày/miệng) khớp đúng cảm xúc của đoạn script tại thời điểm đó. Lỗi thật đã xảy ra ở Tập 4: 27/158 scene hoàn toàn không nhắc tên Gấu trong mô tả (chỉ được chèn tự động qua câu vị trí khung hình chung chung ở cuối), khiến AI tự vẽ Gấu rời rạc, mặt luôn về đúng 1 kiểu mặc định. Trước khi chốt prompt, tự rà: nếu prompt không có ít nhất 1 câu mô tả biểu cảm+hành động cụ thể của Gấu, phải viết bổ sung ngay, không để trống cho AI đoán.
- **charStyle Phần A (nhận diện — đầu, mắt híp, vest xanh navy, kính, răng vàng) phải dán nguyên văn mọi prompt có Gấu, không rút gọn**, kể cả ảnh cận cảnh/crop hẹp. Ngoại lệ duy nhất: khung hình thật sự cắt cụt 1 phần theo bố cục thì ghi rõ lý do trong prompt thay vì âm thầm bỏ mô tả. **KHÔNG dán Phần B (tư thế mặc định của bảng reference sheet gốc)** vào prompt scene thật — Phần B chỉ dùng khi gen lại chính bảng reference sheet.
- Mỗi prompt có Gấu phải có câu chặn ngay sau mô tả nhân vật: `no beard, no mustache, no extra facial hair, no extra accessories, no clothing details beyond what is explicitly described above`.
- Mỗi prompt kết thúc bằng câu chặn chữ/vật thể lạ: `no random or unrelated text, letters, numbers, or logos anywhere in the image beyond what is explicitly described above, no random unrelated objects beyond what is described above, no visible grid lines or ruler marks anywhere in the image`.
- Mọi chữ đọc được trong ảnh phải là **tiếng Việt** hoặc số/ký hiệu tiền tệ — cấm AI tự bịa chữ tiếng Anh dù chung chung (VD "revenue", "profit").
- **Quy tắc bối cảnh Việt Nam + tiền VND (bắt buộc, lỗi thật đã xảy ra ở Tập 4)**: mọi prompt phải dán thêm câu chặn `the setting must be unmistakably Vietnamese (Vietnamese street architecture, Vietnamese shop signage, Vietnamese people), all currency shown is Vietnamese dong with Vietnamese number formatting such as "triệu/tháng", never US dollars or a dollar sign, all legible text in the image is Vietnamese only, no English words, no Western/generic city skyline`. Lý do: bất kỳ scene nào mô tả mơ hồ kiểu "a high number", "handing over cash", "a rental price signboard" mà KHÔNG chỉ rõ số/tiền Việt cụ thể, AI tự mặc định điền tiếng Anh/USD/phố kiểu Mỹ theo thiên lệch huấn luyện — đã xảy ra thật (biển giá hiện "$11,500/month", tiền USD, "LEASE AGREEMENT" tiếng Anh, skyline kiểu Mỹ dù prompt không hề yêu cầu).
- **Mọi số liệu/biển hiệu/giấy tờ xuất hiện trong scene phải có 1 trong 2**: (a) số/chữ tiếng Việt cụ thể được viết rõ trong prompt (VD "35 triệu/tháng"), hoặc (b) mô tả tường minh là mờ/không đọc được ("deliberately blurred, no legible text") — không để mô tả mơ hồ kiểu "a high number"/"an approved amount" mà không chốt rõ 1 trong 2 hướng trên, vì AI sẽ tự bịa số/chữ tiếng Anh khi thiếu chỉ dẫn cụ thể.
- **Quy tắc chặn văn bản/giấy tờ giả mạo cơ quan nhà nước (bắt buộc, lỗi thật đã xảy ra ở Tập 4)**: mô tả mơ hồ kiểu "an official-looking decree document with a red stamp" khiến AI tự vẽ hẳn 1 văn bản hành chính giả có quốc hiệu "CỘNG HÒA XÃ HỘI CHỦ NGHĨA VIỆT NAM", tên cơ quan nhà nước thật (VD "ỦY BAN NHÂN DÂN THÀNH PHỐ..."), con dấu đỏ, và cả đoạn văn pháp lý tiếng Việt vô nghĩa/gãy vụn — rủi ro cao hơn hẳn lỗi chữ tiếng Anh vì trông như giả mạo văn bản nhà nước thật. Mọi prompt có giấy tờ/hợp đồng/biển hiệu phải dán thêm câu chặn: `Any paper, document, contract, sign, or form shown must display ONLY the specific short text or number explicitly described — no government letterhead, no national emblem, no government agency name, no official seal or red stamp, no invented legal paragraphs or additional sentences. Any part without specified text must be blank or blurred non-legible scribble, never filled in with invented text.` Không bao giờ mô tả giấy tờ bằng cụm chung chung "official/decree/government document" mà không khoá rõ nội dung.
  - **Bối cảnh pháp lý (đã tra cứu)**: hành vi này không cấu thành tội "làm giả con dấu, tài liệu của cơ quan, tổ chức" (Điều 341 Bộ luật Hình sự) vì luật yêu cầu có yếu tố *dùng để lừa dối cán bộ/người dân* — ảnh minh hoạ phong cách hoạt hình 2D có mascot Gấu đứng cạnh, nằm trong video đã có disclaimer cố định "tổng hợp/ý kiến cá nhân" (`docs/DNA.md` mục 5) bao trùm toàn bộ nội dung video kể cả ảnh minh hoạ, không ai xem là văn bản thật. Không cần thêm disclaimer/cơ chế nào khác ngoài disclaimer cố định sẵn có — chỉ cần giữ đúng quy tắc kỹ thuật (không quốc hiệu/tên cơ quan/con dấu thật) để ảnh gọn gàng chuyên nghiệp, không phải vì lo ngại pháp lý.
  - Khi cần cảnh có "giấy tờ/con dấu" mang tính minh hoạ chung (không phải văn bản thật cụ thể), có thể thêm cụm mô tả rõ tính chất hư cấu ngay trong prompt để định hướng AI, VD: `a generic fictional illustrative stamp shape (not a real government seal), purely decorative, no readable agency name` — giúp AI vẽ 1 hình con dấu mang tính biểu tượng thay vì cố tái tạo con dấu nhà nước thật.
- **Cấm minh hoạ người bằng bóng đen/silhouette hoặc phong cách siêu nhiên/hư ảo (lỗi thật đã xảy ra ở Tập 8)**: khi scene có nhiều người ở hậu cảnh (đám đông, khách qua đường...) mà không mô tả rõ từng người, AI có xu hướng vẽ tất cả thành bóng đen phẳng lì không mặt/không biểu cảm, đồng thời các đồ hoạ overlay (biểu đồ, chữ số) nếu mô tả mơ hồ kiểu "glowing"/"floating" dễ bị vẽ thành quả cầu phát sáng siêu nhiên lơ lửng giữa trời — lạc hẳn tông flat cartoon nhất quán của kênh. Mọi prompt phải dán thêm câu chặn: `any human figures shown must be fully illustrated in the same flat 2D cartoon style with visible faces, expressions, and clothing details — absolutely no solid black silhouettes, no faceless shadow figures, no ghostly or supernatural rendering, no ethereal glowing outlines on people; any icons or graphic overlays (charts, symbols) must be flat and grounded in the scene, not rendered as glowing supernatural orbs floating in the sky.` Đồng thời khi mô tả chữ/số/biểu đồ overlay, tránh dùng từ "glowing"/"floating softly" — thay bằng mô tả gắn vào 1 vật thể cụ thể trong khung (biển hiệu, bảng phấn, slide đồ hoạ phẳng) để giữ tính "grounded" thay vì lơ lửng vô định.
- **Quy tắc 30 độ**: 2 ảnh liên tiếp của cùng 1 chủ thể phải đổi góc camera tối thiểu ~30 độ hoặc đổi cỡ cảnh rõ rệt — không để 2 ảnh cạnh nhau giống hệt bố cục.
- **Không tái dùng y hệt 1 ảnh/bối cảnh đã gen cho tập trước sang tập khác** — YouTube coi là "reused content", có thể bị hạn chế phân phối/kiếm tiền dù ảnh tự tạo miễn phí. Mỗi tập cần bối cảnh/góc quay dàn dựng riêng, dù dùng chung charStyle/sceneStyle cố định của Gấu.
- Mọi scene `AI gen` (theo cột "Loại ảnh" ở `scene-list.md`) luôn là **1 ảnh AI gen thuần tuý trong 1 phong cách nhất quán** theo bgStyle (`docs/Visual-Prompts-goc.md` mục 2) — KHÔNG tách lớp/ghép ảnh chụp thật ngoài đời, KHÔNG hiệu ứng xoá phông/phủ đen phức tạp. **Ngoại lệ**: scene đánh dấu `Crop nguồn thật` ở C2 — xem cách xử lý riêng ở Bước 3 dưới đây; và scene `Icon động` — xem Bước 3b. Không áp dụng quy tắc "thuần AI gen 1 phong cách bối cảnh" cho 2 loại này.

## Kinh nghiệm thực tế khi viết prompt (rút ra từ thử nghiệm, cân nhắc linh hoạt theo từng scene — không phải checklist ép đủ mọi mục)
- **Bối cảnh phải cụ thể/chi tiết, không mô tả chung chung** (VD "quầy pha chế inox, ly nhựa xếp chồng, bảng giá viết tay dán băng keo" thay vì chỉ "quán trà sữa") — mô tả mơ hồ kiểu "shop backdrop" dễ khiến AI tự chọn nhầm loại bối cảnh khác hẳn (tiệm đồng hồ, bàn văn phòng...) không liên quan ngành nghề tập, phải khoá rõ đúng ngành nghề mỗi khi tả bối cảnh mờ phía sau.
- **Quy tắc cứng — cấm AI tự vẽ hoạ tiết trang trí lấp chỗ trống (lỗi thật đã xảy ra ở Tập 7)**: mô tả nền mơ hồ kiểu "flat illustrated background, plain" hoặc "no props" mà không khoá rõ là màu phẳng đơn sắc, AI có xu hướng tự lấp khoảng trống bằng hoạ tiết trang trí không liên quan (đã xảy ra thật: hoạ tiết mandala/trống đồng, đèn lồng, tre trúc, kiến trúc đền chùa Trung Hoa xuất hiện dù prompt không hề yêu cầu) — nhìn "khó hiểu", lạc tông với bảng màu/bản sắc kênh. Mọi prompt phải dán thêm câu chặn: `Background must be a clean solid/flat color or only the specific illustrated elements explicitly described below — absolutely no decorative mandala or circular ornamental patterns, no temple/pagoda architecture, no lanterns, no bamboo, no religious or cultural iconography, no unrelated linework borders or filler decoration of any kind.`
- **Ưu tiên nền phong-cách-ảnh-thật (mờ nhẹ phía sau) hơn nền phẳng đơn sắc cho scene có Gấu**, kể cả cận cảnh — chỉ dùng nền phẳng 1 màu khi thật sự không có bối cảnh nào hợp lý.
- Chữ trong ảnh: cho phép AI vẽ chữ khi thật sự cần (biển hiệu, bảng số liệu) nhưng **rút ngắn tối đa 3-6 từ** — câu càng dài càng dễ sai chính tả/dấu; chữ nhỏ/ở xa thì mô tả mờ không đọc được thay vì ép đọc rõ. Sau khi gen, kiểm tra chính tả chữ được yêu cầu đọc rõ ở bước QA (`C4-Doi-ten-Kiem-tra-Anh.md`), gen lại riêng ảnh nếu sai.
- **Tránh icon dạng biểu tượng cảnh báo phổ biến kiểu logo** (tia chớp, dấu chấm than trong tam giác...) — dễ bị hệ thống lọc bản quyền/IP hiểu nhầm là nhãn hiệu có sẵn, gây từ chối tạo ảnh khó đoán mà đổi câu chữ khác trong cùng prompt không sửa được; phải đổi hẳn cách diễn đạt cảnh báo (qua biểu cảm/tư thế) mới hết.
- **Tránh bố cục "split composition" (cắt đôi khung hình đúng qua thân nhân vật)** — AI xử lý kém, dễ cắt lìa/lệch màu nhân vật. Cần thể hiện tương phản trước/sau thì đặt nhân vật nguyên vẹn 1 bên, phần tương phản chỉ nằm ở hậu cảnh.
- **Tránh bố cục "cực cận + cắt cúp 1 phần mặt nhân vật"** — có vẻ dễ bị kiểm duyệt từ chối nhầm hơn cỡ cảnh thấy trọn đầu/vai; ưu tiên cận vừa (medium close-up) khi cần tiêu điểm biểu cảm.
- **Khi tay/chi tiết cận cảnh là tiêu điểm chính, nhắc lại tường minh quy tắc nhận diện quan trọng ngay trong mô tả tư thế của scene đó** (VD "rounded fingertips, no claws") — chỉ dựa vào charStyle chung ở đầu prompt không đủ, càng cận cảnh AI càng dễ tự vẽ lệch.
- Tránh dùng từ "grid"/"grid line" khi mô tả bố cục 1/3 — mô tả vị trí bằng lời thường (VD "positioned roughly one third from the right edge, not centered") kèm câu chặn không có đường kẻ lưới.
- Ghi rõ vị trí chủ thể trong khung ở mọi prompt theo quy tắc 1/3 (đa số lệch trái/phải, luân phiên giữa các scene liên tiếp), chỉ để giữa khung khi cần nhấn mạnh (hook mở đầu, thumbnail).
- **Biểu cảm không được cứng đơ 1 kiểu xuyên suốt (lỗi thật đã xảy ra ở Tập 4)**: charStyle Phần A mô tả "mắt híp bán nguyệt, nhếch mép cười khẩy lộ răng vàng" là **hình dáng mặc định lúc nghỉ (resting state)**, KHÔNG phải biểu cảm bắt buộc mọi ảnh — nếu dán nguyên văn mà không chặn rõ, AI có xu hướng lặp lại đúng 1 kiểu mặt nheo mắt cười khẩy ở mọi scene bất kể ngữ cảnh cảm xúc thật (đã xảy ra: scene "bối rối/lác mắt" và scene "ngân hàng nghiêm túc" đều ra cùng 1 biểu cảm cười khẩy). Bắt buộc thêm ngay sau charStyle Phần A câu ghi đè: `IMPORTANT: the eye shape, eyebrow angle, and mouth shape described above are Gau's default resting state ONLY — the specific emotional expression described in the scene action below MUST clearly override this default (eyes can open wide and round for shock/surprise, eyebrows can shoot up for confusion or knit together for worry/anger, mouth can hang open, frown, or grin broadly instead of the default smirk). Do not render the same sly narrow-eyed smirk in every image regardless of context.`
- **Mỗi scene phải viết biểu cảm cụ thể, sống động, hơi phóng đại theo đúng cảm xúc trong script tại thời điểm đó** (VD không viết chung chung "concerned expression" mà viết "eyes wide open in visible alarm, mouth hanging open, one eyebrow shot up") — tránh dùng lại đúng 1 vài tính từ cảm xúc mòn (concerned, thoughtful, confident) cho nhiều scene liên tiếp, đối chiếu diễn biến cảm xúc thật trong `voice-script.md`/`script.md` tại đoạn đó trước khi chọn từ.

## Bước 0a — Research bối cảnh hiện đại TRƯỚC khi dàn dựng (bắt buộc, làm 1 lần cho cả tập)
**Lý do**: charStyle/bgStyle gốc không tự khoá "thời đại" — nếu chỉ mô tả chung chung kiểu "phố Sài Gòn, quán trà sữa", AI có thiên lệch huấn luyện vẽ ra Việt Nam kiểu cũ/hoài cổ (biển hiệu sơn tay, xe đạp, kiến trúc thập niên 80-90) thay vì không khí thật hiện tại — không đúng đối tượng khán giả kênh đang sống ở đô thị hiện đại.

Trước khi liệt kê danh sách set ở Bước 0, với MỖI set bối cảnh dự kiến dùng trong tập (quán trà sữa, tiệm rửa xe, phòng gym, phố xá...):
1. Dùng WebSearch/WebFetch tra cứu ảnh/bài viết thật mô tả đúng loại bối cảnh đó ở Việt Nam hiện tại (ưu tiên Sài Gòn/TP.HCM nếu chủ đề không chỉ rõ vùng khác) — tìm đặc điểm hình ảnh cụ thể: kiểu biển hiệu/bảng giá (font, màu, LED hay in bạt), loại xe máy phổ biến hiện nay, trang phục người trẻ/nhân viên, vật liệu quán xá (kính cường lực, đèn neon, ốp gỗ công nghiệp...), mật độ dây điện/vỉa hè, ánh sáng đô thị ban đêm...
2. Ghi lại thành các cụm mô tả CỤ THỂ (không chung chung) để đưa thẳng vào mô tả set ở Bước 0 — VD thay vì "quán trà sữa" viết "quán trà sữa mặt tiền kính cường lực, biển hiệu đèn LED neon hồng-tím, menu dán decal trên tường, ghế nhựa Bàn Ghế Xuân Hoà, xe máy tay ga đậu vỉa hè, dây điện chằng chịt phía trên".
3. Đây là bước **tra cứu để lấy chi tiết hiện thực, không phải sao chép/mô tả lại 1 địa điểm/thương hiệu thật cụ thể nào** — vẫn giữ khung "hư cấu tổng hợp" theo an toàn nội dung ở `CLAUDE.md` mục 2.
4. Việc CHỌN chi tiết nào trong số tìm được để đưa vào prompt là linh hoạt tuỳ set/tuỳ tập — không có 1 công thức cố định, tránh mọi tập đều na ná nhau.
5. **Chọn 1 thời điểm trong ngày (sáng/trưa/chiều/tối) phù hợp nhất với đúng bản chất ngành nghề đang phân tích, rồi giữ NHẤT QUÁN thời điểm đó xuyên suốt mọi set của tập** (lỗi thật đã xảy ra ở Tập 8: ánh sáng ban ngày/ban đêm lẫn lộn không lý do giữa các scene, nhìn rời rạc, không đúng không khí hoạt động thật của ngành). VD: ngành hoạt động chủ yếu về đêm (xe đẩy đồ ăn vặt, quán nhậu, quán cà phê vỉa hè tối, karaoke...) nên chọn bối cảnh ban đêm xuyên suốt; ngành hoạt động ban ngày (văn phòng, ngân hàng, trường học...) nên chọn ban ngày. Nếu cốt truyện có lý do thật để đổi thời điểm (VD case nhân vật từ sáng đến tối), phải nêu rõ lý do chuyển cảnh trong mô tả, không đổi tuỳ tiện giữa các scene liền kề.

## Bước 0 — Đóng vai đạo diễn, dựng bối cảnh xuyên suốt TRƯỚC khi viết prompt (bắt buộc, làm 1 lần cho cả tập)
**Không viết prompt ngẫu hứng từng scene riêng lẻ.** Trước khi viết bất kỳ prompt nào, đọc lại toàn bộ `script.md` + `scene-list.md`, đóng vai đạo diễn lên kế hoạch dàn dựng (staging) có mạch nối nhất quán từ đầu đến cuối video:
1. **Liệt kê danh sách "bối cảnh/set" cố định của cả tập** (VD: quán cà phê vỉa hè, trục phố thương mại Hà Nội, văn phòng ngân hàng, không gian gia đình, phố Sài Gòn, gian hàng pop-up...) — mỗi set có mô tả chi tiết cố định (kiến trúc, đạo cụ, ánh sáng) dùng lại xuyên suốt mọi scene thuộc set đó, không mô tả lại từ đầu mỗi lần dùng.
2. **Liệt kê danh sách nhân vật ẩn danh xuất hiện lặp lại** (chủ shop, chủ nhà, nhân viên ngân hàng, môi giới, người lớn tuổi...) — mỗi nhân vật có 1 mô tả cố định (tuổi, trang phục, kiểu tóc) dùng đúng 1 câu y hệt ở mọi scene họ xuất hiện, để AI gen ra đúng 1 người xuyên suốt thay vì mỗi ảnh 1 gương mặt khác.
3. **Vẽ mạch nối giữa các set** theo đúng thứ tự script — set nào chuyển sang set nào, có lặp lại set cũ ở đoạn kết không (VD quay lại đúng góc làm việc mở đầu để tạo cảm giác khép vòng) — giữ tính nhất quán không gian như 1 bộ phim thật, không phải các ảnh rời rạc.
4. Ghi lại toàn bộ kế hoạch này thành 1 bảng/section riêng trong `scene-list.md` hoặc file riêng trước khi bắt đầu viết `prompt-anh.txt` — dùng làm căn cứ tra cứu khi viết từng prompt ở Bước 2, đảm bảo mọi scene cùng set/cùng nhân vật dùng đúng 1 mô tả nguồn duy nhất (không viết lại diễn đạt khác mỗi lần).

## Bước 1 — Chọn cảnh cho từng scene
Với mỗi scene trong `scene-list.md`:
- Minh hoạ đúng nội dung đoạn đó. Gấu đóng vai trò như 1 "phóng viên" đứng tại hiện trường minh hoạ (tính toán/chỉ vào biểu đồ/dò xét qua kính...).
- **Scene thuần biểu đồ/số liệu/so sánh**: nếu có Gấu thì để biểu cảm đơn giản, trọng tâm dồn vào nội dung minh hoạ chứ không phải Gấu. Chỉ bỏ hẳn Gấu khi cảnh thật sự không cần ai đứng cạnh.
- Đánh dấu riêng 1-2 scene có bố cục bắt mắt nhất (Gấu rõ nét, có vùng trống để đè chữ) làm ứng viên thumbnail — xem `QuyTrinh/C7-Thumbnail.md`.
- Trước khi xuất `prompt-anh.txt`, rà lại toàn bộ danh sách bối cảnh của các scene trong CÙNG 1 tập — đổi góc không gian/đồ vật khác nhau, trừ khi nội dung thật sự tiếp diễn cùng 1 khoảnh khắc.

## Bước 1.5 — Hoá trang theo tập (tuỳ chọn sáng tạo, chỉ làm khi cần)
Mặc định Gấu giữ nguyên vest xanh navy mọi tập. Nếu tập này có 1 tình huống đủ hợp để Gấu hoá thân thành nhân vật trong câu chuyện (VD nhân viên pha chế, khách hàng, tài xế...) và thấy đáng làm để tăng sức hút, hỏi người dùng xác nhận trước khi làm. Nếu đồng ý:
1. Viết 1 prompt "ảnh tham chiếu hoá trang" riêng cho tập này: giữ nguyên phần nhận diện gương mặt (đầu, mắt híp, răng vàng, màu lông), chỉ đổi trang phục/đạo cụ theo vai diễn — sinh reference sheet tương tự `Fanpage-Asset/gau_mascot.jpg` gốc.
2. Lưu vào `Bai-Dang/Tap N - [tên]/gau-hoa-trang-ref.jpg`, dùng làm tham chiếu xuyên suốt cho MỌI scene hoá trang trong tập đó.
3. Các scene khác trong CÙNG tập không cần hoá trang vẫn có thể quay lại vest gốc — không bắt buộc hoá trang xuyên suốt cả video.

## Bước 1.6 — Kho kỹ thuật hình ảnh làm ảnh tĩnh sống động (chọn linh hoạt theo scene, không phải quay vòng cố định)
Ảnh tĩnh không animation/zoom/pan, nên "sức sống thị giác" phải đến từ góc máy, bố cục, ánh sáng — không lặp mãi 1 kiểu đứng thẳng nhìn thẳng camera. Đây là kho lựa chọn, mỗi scene tự cân nhắc dùng gì, không ép đủ mọi kỹ thuật vào 1 tập và không dùng đúng 1 trình tự cố định giữa các tập:

**Cỡ cảnh:** đại cảnh/thiết lập → toàn cảnh → trung cảnh → cận vừa → cận mặt → cực cận đặc tả (mắt, tay, chi tiết vật thể/con số).

**Góc máy — chọn theo cảm xúc muốn tạo:** ngang tầm mắt (trung tính); góc thấp ngước lên (uy quyền/áp lực, hợp chi phí lớn/rủi ro); góc cao nhìn xuống (nhỏ bé/bất lực, hợp thất bại/thua lỗ); góc nghiêng lệch trục (bất ổn, hợp rủi ro/bất thường); góc chim bay (tổng quan mô hình/sơ đồ); qua vai (quan sát 1 tình huống).

**Bố cục tạo cảm giác chuyển động dù ảnh đứng yên:** đường chéo dẫn mắt thay vì đối xứng tĩnh; lấy khung qua 1 lớp tiền cảnh mờ; dáng đứng như "đóng băng giữa chuyển động" (đang bước, tay đang vung) thay vì đứng yên hoàn toàn; tương phản sáng-tối/rim light tách lớp chủ thể khỏi nền.

Cross-check với quy tắc 30 độ: khi luân phiên góc/cỡ cảnh giữa các scene liên tiếp, ưu tiên lấy từ kho kỹ thuật này thay vì chỉ đổi qua đổi lại 2-3 kiểu quen thuộc.

## Bước 2 — Viết prompt

**charStyle cố định — CHỈ phần nhận diện, dán nguyên văn mọi prompt có Gấu — bản ĐÃ CHỨNG MINH chạy ổn định thật ở Tập 7 (KHÔNG dùng bản dài dòng ở `docs/Visual-Prompts-goc.md` nữa — bản đó là bản mô tả gốc/tham khảo, không phải bản đã tối ưu cho việc gen scene thật):**
```
Gau, a stylized 2D cartoon mascot character, rounder compact silhouette (head-to-body ratio ~40-45%, soft rounded torso and limbs), consistent solid dark chocolate-brown fur (#4A3728), narrow crescent-shaped squinting sly eyes with glinting black pupils and slanted thick eyebrows, moderately wide mouth with a smug smirk revealing a single glinting gold tooth, small black triangular nose, oversized white 4-fingered cartoon gloves, wearing a fitted navy blue business suit vest (#0C447C), navy bow tie, crisp white dress shirt, round bronze-gold wire-rim glasses (#BA7517).
```

Sau đoạn charStyle trên, mỗi prompt PHẢI viết tiếp riêng 1 cụm mô tả **tư thế + biểu cảm dành riêng cho scene đó** — không để trống, không tái dùng y hệt cụm tư thế của scene khác trong cùng 1 tập. Biểu cảm nên phong phú/hơi phóng đại kiểu meme theo nội dung từng đoạn (mắt trợn kinh ngạc, cười sằng sặc, ôm đầu giả vờ đau khổ...) thay vì mặc định lặp lại 1 dáng.

**sceneStyle cố định (dán nguyên văn mọi prompt) — bản ĐÃ CHỨNG MINH chạy ổn định ở Tập 7:**
```
Flat 2D cartoon-comic style, thick even black outline 3-4px, flat coloring minimal gradient, primary color palette navy blue - gold bronze - white - black.
```
**Quy tắc cứng quan trọng — KHÔNG lặp lại thông tin tỉ lệ cơ thể/chi tiết nhận diện ở cả sceneStyle LẪN charStyle**: chỉ nói 1 lần duy nhất ở charStyle (head-to-body ratio ~40-45%), sceneStyle chỉ nói phong cách vẽ chung (flat, outline, màu). **Lỗi thật đã xảy ra 2 lần liên tiếp ở Tập 8**: lần 1 dùng bản sceneStyle cũ nói "tall, lean, statuesque body ~30%" mâu thuẫn trực tiếp với charStyle "rounder compact ~40-45%"; lần 2 tự sửa sceneStyle thành bản DÀI hơn, lặp lại y hệt thông tin tỉ lệ + găng tay + biểu cảm đã có sẵn trong charStyle — cả 2 lần đều ra ảnh sai/lệch dáng khi so với ảnh tham chiếu `gau_mascot.jpg`. Đối chiếu lại đúng prompt đã dùng ở Tập 7 (đã đăng, đã chạy ổn định nhiều scene) mới phát hiện: bản chuẩn KHÔNG lặp lại thông tin, ngắn gọn, mỗi chi tiết chỉ xuất hiện đúng 1 lần. **Bài học chung: khi nghi ngờ charStyle/sceneStyle cố định có vấn đề, luôn đối chiếu với prompt THẬT đã dùng ở tập trước đã đăng thành công (`Bai-Dang/Tap N/prompt-anh.txt`) trước khi tự sửa theo suy luận lý thuyết — không suy luận rồi sửa trước, không tự ý revert về bản chưa từng chứng minh chạy tốt.**

Mỗi prompt hoàn chỉnh = sceneStyle + charStyle (nếu có Gấu) + bối cảnh cụ thể + tư thế/hành động + chi tiết minh hoạ ý của đoạn, áp dụng đủ các nguyên tắc cứng và kinh nghiệm ở trên.

**Quy tắc cứng — luôn ghi rõ tỉ lệ khung hình (lỗi thật đã xảy ra ở Tập 7):** mọi prompt, không phân biệt có Gấu hay chỉ có nhân vật phụ, phải kết thúc bằng cụm tường minh `16:9 widescreen aspect ratio, landscape orientation` — không được bỏ trống rồi mặc định trông chờ tool tự áp đúng tỉ lệ, vì đã xảy ra thật việc thiếu cụm này khiến ảnh bị gen ra 1:1 (vuông) thay vì 16:9.

## Bước 3 — Xử lý scene `Crop nguồn thật` (thay cho viết prompt AI)
Với mỗi scene đánh dấu `Crop nguồn thật` ở `scene-list.md`:
1. Tra đúng dòng số liệu đã trỏ tới trong `so-lieu-xac-nhan.md`, lấy cột **"Trích dẫn nguyên văn để Ctrl+F định vị đoạn crop"** — không dùng scene này nếu dòng đó vẫn còn đánh dấu `⚠️` (nghĩa là chưa xác nhận nguyên văn), phải xử lý xong mục 7.1 ở `C1-Script.md` trước.
2. Ghi hướng dẫn crop vào file riêng `Bai-Dang/Tap N - [tên]/crop-nguon.md` (thay cho dòng prompt AI ở `prompt-anh.txt` — 2 file này bổ sung nhau theo đúng thứ tự STT, không trộn chung 1 file vì bản chất thao tác khác nhau: 1 bên là prompt gen AI, 1 bên là hướng dẫn thao tác thủ công). Mỗi dòng gồm: **STT (khớp tên ảnh 0NN.jpg cần lưu) — Link nguồn — Trích dẫn cần Ctrl+F — Ghi chú khung crop** (VD "chỉ crop đúng đoạn văn bản chứa số liệu, không chụp nguyên trang, ưu tiên khung có logo/tên báo trong ảnh nếu bố cục trang cho phép").
3. **Khung nền chuẩn cho ảnh crop — dùng chung 1 file cố định, KHÔNG gen mới mỗi tập**: `Fanpage-Asset/Khung-Crop-Nguon/prompt.txt` là prompt đã chốt (Gấu đứng góc trái dưới, tay chỉ vào khoảng trống bên phải, nền phẳng đơn sắc navy để trống 65-70% khung hình). Gen ảnh nền này **1 LẦN DUY NHẤT**, lưu thành `Fanpage-Asset/Khung-Crop-Nguon/khung-nen.jpg`, dùng lại xuyên suốt mọi tập có scene `Crop nguồn thật` — người dùng tự chèn ảnh crop đã chụp vào khoảng trống bên phải khung này bằng CapCut/Photoshop, xuất thành ảnh scene hoàn chỉnh trước khi bỏ vào `Anh Video/`.
   - **Đây là ngoại lệ có chủ đích với quy tắc "không tái dùng ảnh/bối cảnh giữa các tập"** (`docs/chien-luoc-youtube.md` mục 8): khung nền này đóng vai trò như 1 khung trích dẫn/lower-third cố định của kênh (tương tự logo, khung intro/outro), KHÔNG phải ảnh minh hoạ nội dung chính của tập — mỗi tập vẫn có nội dung thật khác nhau được chèn vào (ảnh crop số liệu khác nhau mỗi lần), nên không rơi vào diện "reused content" mà chính sách YouTube nhắm tới (nội dung lặp lại không đổi mới). Nếu sau này cảm thấy khung này xuất hiện quá thường xuyên/nhàm, có thể quay lại làm thêm 1-2 biến thể khác (VD Gấu đứng góc phải, hoặc đổi màu nền) — không bắt buộc chỉ có đúng 1 khung duy nhất mãi mãi.
4. Nếu scene `Crop nguồn thật` cũng cần Gấu xuất hiện cùng khung (VD Gấu đang chỉ vào màn hình điện thoại chứa ảnh crop), tách thành 2 lớp: **ảnh nền Gấu vẫn viết prompt AI riêng như bình thường** (đổi "Loại ảnh" scene đó thành `AI gen` ở C2 vì lúc này Gấu là chủ thể chính), còn ảnh crop chỉ dùng khi KHÔNG cần Gấu trong khung — tránh nhầm lẫn 2 loại vào chung 1 scene.

## Bước 3b — Scene `Icon động`: viết prompt icon minh hoạ (file riêng `prompt-icon.txt`)
Từ bước này C3 xuất **2 file prompt tách biệt theo "Loại ảnh"** (xem Bước 4): `prompt-anh.txt` cho hoạt cảnh minh hoạ (có Gấu), `prompt-icon.txt` cho icon minh hoạ. Scene `Icon động` KHÔNG viết chung vào `prompt-anh.txt` vì phong cách, nền và cách xử lý hậu kỳ khác hẳn.

**Phong cách icon minh hoạ (chốt ngày 21/09/2026, theo mẫu người dùng đưa): đồ hoạ 2D phẳng kiểu infographic hoạt hình, nét viền đen đậm, vài vật thể ghép thành 1 sơ đồ giải thích 1 ý, có nhãn tiếng Việt ngắn trên biển gỗ/thẻ kem.** Gen trên **nền trắng tinh** (để tách nền), rồi C5 ghép lên **nền giấy kẻ ô dùng chung** `Fanpage-Asset/Nen-Luoi-O/nen-luoi-o.png` — nền kẻ ô KHÔNG bao giờ đưa vào prompt (sẽ làm tách nền hỏng).

**Khối style cố định (ghi cứng — nhận diện icon của kênh, dùng nguyên văn cho mọi icon, chỉ thay phần `Composition:` cuối dòng):**
```
Clean flat 2D cartoon infographic illustration in a bold sticker-like vector look: thick even black outlines (about 4px), flat vivid colors with minimal shading, simple friendly rounded shapes, palette of navy blue (#0C447C), bronze-gold (#BA7517), fresh green, warm red and cream as accent colors. A small composition of 2 to 4 clearly separated elements that explain ONE idea, with short Vietnamese labels (2 to 4 words each) written in bold black hand-lettered sans-serif on wooden signs, cream tags or cards. Pure flat white background (#FFFFFF), no grid, no paper texture, no ground, no floor, no drop shadow, no gradient, generous empty margin around the composition. Use cream, beige or light grey (never pure white) for paper, cards and labels so they stay distinct from the background. Any people shown are simple cartoon figures with faces and expressions, never silhouettes; no bear mascot, no animal character. All legible text is Vietnamese only with exact diacritics, currency is Vietnamese dong written like 199.000đ, never dollars; no other text, letters, numbers or logos beyond what is explicitly described. 16:9 widescreen aspect ratio, landscape orientation. Composition: <MÔ TẢ BỐ CỤC>
```
- **Mỗi dòng `prompt-icon.txt` = khối style trên + `Composition: ...` của đúng 1 scene** (1 dòng, không xuống dòng giữa chừng).
- **Phần `Composition:` (CỐT LÕI — viết riêng từng icon, không rập khuôn):** chọn 1 phép ẩn dụ/sơ đồ hợp ý scene (cầu thang tăng dần, cây ra tiền, cán cân, bóc lớp, cột tiền cao thấp, biển chỉ đường, kính lúp...) — mỗi tập/mỗi icon nên khác nhau. Liệt kê rõ từng vật thể, vị trí trái/phải, và **nguyên văn từng nhãn chữ** (2-4 từ, có dấu đầy đủ, số tiền dạng `199.000đ`). Càng ít chữ càng ít lỗi chính tả; nhãn không cần thiết thì bỏ.
- **Quy tắc tách nền:** dùng kem/be/xám nhạt cho giấy, thẻ, nhãn (không dùng trắng tinh trong vật thể — sẽ bị tách nhầm thành nền); các vật thể tách rời nhau, không chạm mép khung.
- **Người trong icon:** dùng nhân vật hoạt hình đơn giản có mặt và biểu cảm, không bóng đen; KHÔNG dùng Gấu (Gấu chỉ ở scene `AI gen`).
- Chữ/số cần đúng tuyệt đối (số liệu) phải kiểm ở C4 sau khi gen; sai thì gen lại riêng ảnh đó.
- Ngoại lệ với quy tắc tỉ lệ 16:9 ở Bước 2: khối style đã có cụm 16:9; không cần thêm câu chặn chữ/tiền Việt/nền như prompt hoạt cảnh (đã gộp trong khối style).
- Các icon 3D photoreal nền trắng làm thử ở Tập 9 (`icon-3d.md`) là phong cách cũ, đã thay bằng phong cách này.

## Bước 3c — Scene `Ảnh thật`: prompt chung phục hồi ảnh + ghép Gấu (file riêng `prompt-anh-that.txt`)
Scene `Ảnh thật` (người dùng tự tìm ảnh chụp thật, xem `C2-Chia-Scene.md`) KHÔNG viết chung vào `prompt-anh.txt` — bản chất là chỉnh sửa/ghép trên 1 ảnh có sẵn, không phải gen từ đầu.

**Thư mục tham chiếu**: tạo `Bai-Dang/Tap N - [tên]/Anh Tham Chieu/` ngay ở C2/C3, copy sẵn ảnh Gấu (`Fanpage-Asset/gau_mascot.png` → `Anh Tham Chieu/gau.png`). Người dùng bỏ ảnh thật vào đây. (Khác thư mục `Anh Tham Chieu Nhan Vat/` của C1.5 — thư mục đó chứa ảnh tạo hình nhân vật phụ.)

**Đặt tên ảnh thật = `sNNN` (chữ `s` + STT scene 3 chữ số): `s002.png`, `s017.jpg`...** — KHÔNG đặt tên số trần (`2.png`, `10.png`): tool gen tự khớp ảnh theo "từ khoá = tên tệp xuất hiện trong dòng prompt" (Cách 4, không phân biệt hoa thường, bất kỳ từ đơn nào trong tên tệp đều khớp), số trần dễ khớp nhầm với số khác trong câu lệnh (16:9, 4K, 30K...) và nạp sai ảnh. Nếu tool có Cách 5 (thẻ `@`) thì dùng `@sNNN` cho tường minh hơn.

**Cấu trúc mỗi dòng `prompt-anh-that.txt` (1 dòng = 1 scene `Ảnh thật`, đúng thứ tự STT, tiếng Anh, không dòng trống):**
1. **Từ khoá ảnh**: `Use the reference photo sNNN as the base image.` — mỗi dòng chứa ĐÚNG 1 từ khoá `sNNN` của scene đó, tuyệt đối không xuất hiện `sNNN` của scene khác; tên ảnh gen ra cũng mang từ khoá này nên C4 ánh xạ trực tiếp.
2. **Phục hồi ảnh (nguyên văn giống nhau mọi dòng):** phục hồi, làm nét, tăng độ phân giải, đưa về khung 16:9 (mở rộng/cắt khung, giữ nguyên chủ thể chính); vẽ lại mọi khuôn mặt người thành mặt mờ ẩn danh; xoá mọi logo, tên thương hiệu, tên bảng hiệu, số điện thoại, địa chỉ, watermark rồi lấp nền tự nhiên; giữ nguyên số tiền/giá đọc được.
3. **Ghép Gấu (mặc định có, người dùng chốt 24/09/2026):** dùng ảnh tham chiếu `gau` — Gấu giữ 2D cartoon nét viền dày, ảnh nền giữ photoreal, khớp phối cảnh/tỉ lệ/hướng sáng, có bóng đổ nhẹ; nhắc lại Character Lock (Phần A charStyle, KHÔNG dán "Default pose" Phần B); tư thế + biểu cảm + vị trí đứng riêng từng scene theo nội dung.
4. **Nội dung ảnh (`Photo content:`)**: mô tả ảnh thật cần có ở scene đó (khớp ảnh người dùng đã tìm) + chỗ Gấu đứng.
5. Kết: `no extra text, no captions, no watermark, no additional characters, 16:9.`

**Bài học Gấu bị gen "thành con khác" (Tập 10, scene ảnh đông người):** câu "biểu cảm cảnh đè lên biểu cảm mặc định" + biểu cảm vui phóng đại + ảnh nhiều người dễ làm tool vẽ lại cả mặt/dáng Gấu. Khi gặp: (a) đặt Gấu ở khoảng trống rõ ràng, thấy trọn người, nét viền tách khỏi đám đông; (b) biểu cảm chỉ đổi tư thế/miệng, **mắt vẫn híp hình trăng lưỡi liềm**; (c) thêm câu khoá: "Gau MUST look exactly like the character in the reference image gau: same fur color, face, navy vest, bow tie, gold glasses, white gloves; do not redesign him, do not turn him into a human". Nếu 1 dòng đã sai, sửa riêng dòng đó rồi gen lại; nếu đúng thì thêm câu khoá vào mọi dòng.

## Bước 4 — Xuất các file prompt: `prompt-anh.txt`, `prompt-icon.txt` và `prompt-anh-that.txt`
Tách theo cột "Loại ảnh" của `scene-list.md`. **Mỗi file chỉ chứa các dòng của đúng loại của nó, theo đúng thứ tự STT tăng dần, KHÔNG có dòng trống chèn cho scene loại khác** (đổi so với trước: không còn giữ dòng rỗng cho scene crop — tool gen ảnh vốn bỏ qua dòng trống và đánh số theo dòng có prompt; việc ánh xạ dòng ↔ STT thật do C4 làm theo `scene-list.md`):
- **`prompt-anh.txt`** — hoạt cảnh minh hoạ: mỗi dòng = 1 prompt hoàn chỉnh của 1 scene `AI gen`, nằm gọn trên 1 dòng, đúng thứ tự STT. Không có số thứ tự/tiêu đề đầu dòng.
- **`prompt-icon.txt`** — mỗi dòng = 1 prompt icon của 1 scene `Icon động` theo Bước 3b, đúng thứ tự STT.
- **`prompt-anh-that.txt`** — mỗi dòng = 1 prompt của 1 scene `Ảnh thật` theo Bước 3c, đúng thứ tự STT; chỉ tạo nếu tập có scene `Ảnh thật`.
- Scene `Crop nguồn thật` không có dòng ở file nào (thông tin nằm ở `crop-nguon.md`, Bước 3).
- Xuất kèm file **`anh-xa-prompt.md`** (cùng thư mục tập): mỗi file prompt có bao nhiêu dòng và bảng dòng N ↔ STT scene (để C4 và người dùng đối chiếu); khi báo người dùng cũng nêu ngắn gọn số dòng từng file. Nếu tập không có scene `Icon động` thì không tạo `prompt-icon.txt`. Tương tự không có `Ảnh thật` thì không tạo `prompt-anh-that.txt` và thư mục `Anh Tham Chieu/`.

**Vị trí lưu file**: cả 2 file ở gốc `Bai-Dang/Tap N - [tên]/` — KHÔNG để trong `Anh Video/`.

Ngay sau khi xuất, **tạo sẵn thư mục rỗng**: `Anh Video/` (ảnh hoạt cảnh gen + ảnh crop) và, nếu có icon, `Icon Goc/` (ảnh icon nền trắng vừa gen, chưa tách nền) để người dùng có chỗ lưu ngay khi gen xong.

## Bước 5 — Tạo ảnh & ghép
**Hướng dẫn gen (nhắc người dùng khi giao 2 file prompt):**
| File | Gen ra | Bỏ ảnh vào |
|---|---|---|
| `prompt-anh.txt` | Hoạt cảnh có Gấu (`AI gen`) | `Anh Video/` |
| `prompt-icon.txt` | Icon minh hoạ nền trắng (`Icon động`) | `Icon Goc/` |
| `prompt-anh-that.txt` | Ảnh thật đã phục hồi + ghép Gấu (`Ảnh thật`), nạp ảnh từ `Anh Tham Chieu/` | `Anh Video/` (tên file gen chứa từ khoá `sNNN`) |
- Dán **từng dòng** vào tool gen, mỗi dòng là 1 prompt hoàn chỉnh (không tách/cắt); **giữ nguyên thứ tự dòng** — ảnh thứ N ứng với dòng N, C4 dựa vào `anh-xa-prompt.md` (bảng dòng ↔ STT) để đổi tên.
- Prompt icon phải ra **nền trắng tinh** (điều kiện để tách nền); nếu tool ra nền khác thì báo lại để sửa prompt.
- Nên **gen thử 6-8 dòng trải đều mỗi file, kiểm chất lượng (nhất là chữ tiếng Việt, số tiền) rồi mới gen phần còn lại**; dòng nào lỗi thì viết lại prompt và gen lại riêng dòng đó.
- Claude tạo sẵn 2 thư mục `Anh Video/` và `Icon Goc/` ngay khi xuất prompt (Bước 4) và xuất kèm `anh-xa-prompt.md` (mỗi file prompt: dòng N ↔ STT scene).

Người dùng gen ảnh từ 2 file prompt, bỏ ảnh hoạt cảnh vào `Anh Video/`, ảnh icon vào `Icon Goc/`, và tự crop ảnh nguồn thật theo `crop-nguon.md` (bỏ vào `Anh Video/`). Sau đó `QuyTrinh/C4-Doi-ten-Kiem-tra-Anh.md` đổi tên từng loại theo STT, tách nền icon và **ghép tất cả thành 1 bộ ảnh cuối duy nhất trong `Anh Video/`** (mỗi STT đúng 1 file) trước khi qua timing/xuất clip.
